# Server Configuration
# Backend will run on 5001 to avoid conflicts and match the frontend env.
PORT=5001
NODE_ENV=development
API_BASE=http://localhost:5001

# Database Configuration (MongoDB Atlas)
# Ensure your IP is allowed in Atlas Network Access.
MONGODB_URI=mongodb+srv://securebank:Aa123123Aa%24@securebnk.0am4siz.mongodb.net/securebank?retryWrites=true&w=majority

# Seed demo users into MongoDB (set true to enable on startup)
DEMO_SEED=true
# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_min_32_characters_long_change_in_production
JWT_EXPIRE=15m
JWT_REFRESH_SECRET=your_refresh_token_secret_min_32_characters_long

# Encryption
ENCRYPTION_KEY=your_32_character_encryption_key_here_exactly_32_chars

# CORS
CLIENT_ORIGIN=http://localhost:3001

# API Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
# Session Configuration
SESSION_SECRET=your_session_secret_key_here